import 'package:djd_kids/model/enums.dart';
import 'package:flutter/cupertino.dart';

class AddCharacterForm extends StatelessWidget {
  final TeamType teamType;

  const AddCharacterForm({Key? key, required this.teamType}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Construisez le formulaire pour ajouter un personnage ici
    return Container(); // Remplacez par votre propre widget
  }
}